*.iml
.gradle/
.idea/
.kotlin/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
*.hprof
.DS_Store

# gradle wrapper jar — se genera (o repara) solo al abrir el proyecto en
# Android Studio, o corriendo `gradle wrapper` con un Gradle instalado — ver
# README para el porqué no está commiteado.
gradle/wrapper/gradle-wrapper.jar

# core/database/schemas/ SÍ se commitea (no se ignora) — Room lo exporta ahí
# en cada build (ksp room.schemaLocation) y hace falta el historial completo,
# desde la v1, para poder escribir tests de Migration más adelante.
