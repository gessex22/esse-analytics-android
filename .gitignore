*.iml
.gradle/
.idea/
.kotlin/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
*.hprof
.DS_Store

# gradle wrapper jar — se genera (o repara) solo al abrir el proyecto en
# Android Studio, o corriendo `gradle wrapper` con un Gradle instalado — ver
# README para el porqué no está commiteado.
gradle/wrapper/gradle-wrapper.jar

# Room exporta acá el schema de cada versión — sí se commitea a partir de
# cuando exista una v2 real con Migration (ver EsseAnalyticsDatabase.kt).
